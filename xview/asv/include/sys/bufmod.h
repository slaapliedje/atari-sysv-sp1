/*
 * sys/bufmod.h - Atari System V has no STREAMS buffer module. XView pushes
 * bufmod only when this header defines SB_NO_DROPS; left empty, it does not.
 */
#ifndef _SYS_BUFMOD_H
#define _SYS_BUFMOD_H
#endif
